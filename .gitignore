node_modules/
dist/

# public/market/ is generated by `npm run market` from auctions.db, but it is
# committed on purpose: the Pages runner has no access to the database, so
# anything not in the repo is simply missing from the deployed site.
tools/__pycache__/
